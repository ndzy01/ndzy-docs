---
title: git
order: 2003
---

```bash
git branch -a
git checkout -b gh-pages remotes/origin/gh-pages
git branch -vv
```

```bash
git stash save "暂存一下"
git stash pop
git stash apply 默认应用最近一次 stash
git stash show -p
```
